import { CharacternamePricePipe } from './charactername-price.pipe';

describe('CharacternamePricePipe', () => {
  it('create an instance', () => {
    const pipe = new CharacternamePricePipe();
    expect(pipe).toBeTruthy();
  });
});
