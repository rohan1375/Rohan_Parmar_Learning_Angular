import { CharacterPowerPipe } from './charactername-power.pipe';

describe('CharacternamePowerPipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
